# Gradle settings
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.caching=true

# Mod properties
mod.id=flight_display
mod.name=Critical Flight Display
mod.version=2.0.0
mod.group=net.critical
mod.description=Adds a HUD overlay with pitch, horizon ladder, and speed while Elytra flying

# Publishing (configure secrets in CI/CD)
# curseforge.id=YOUR_CURSEFORGE_PROJECT_ID
# modrinth.id=YOUR_MODRINTH_PROJECT_ID
